# Implement the following plan

Follow the `Instructions` to implement the `Plan` then `Report` the completed work.

## Instructions

- Read the plan, think hard about the plan and implement the plan.
- If any web search is needed, perform the web search and save the results in the folder: `ai_docs/web_search_results/` with a relevant name and used as a local cache for future use.

## Plan

$ARGUMENTS

## Report

- Summarize the work you've just done in a concise bullet point list.
- List all modified/created files with their absolute paths
- Report the files and total lines changed with `git diff --stat`
